[package]
name = "dosh-application"
version = "0.1.0"
edition = "2024"

[dependencies]
dosh-domain = { path = "../dosh-domain" }

[dev-dependencies]
tokio = { version = "1.50.0", features = ["macros", "rt"] }
