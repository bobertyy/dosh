[package]
name = "dosh-domain"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.45", default-features = false, features = ["std"] }
thiserror = "2.0.17"
uuid = { version = "1", features = ["v4"] }
